---
import {
  Target,
  CursorClick,
  PlugsConnected,
  BookBookmark,
  SignIn,
  EnvelopeOpen,
  Envelope,
  AirplaneTakeoff,
  HandWaving,
  Timer,
  AirplaneTilt,
  Gauge,
  Video as VideoIcon,
  DiamondsFour,
} from '@phosphor-icons/react';
import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import Logo from '../components/Logo.astro';
import Gradient from '../components/Gradient.astro';
import Chart from '../components/Chart.astro';
import arrow from '../images/arrow.svg';
import pipeline from '../images/pipeline.svg';
import Flow from '../components/Flow.astro';
import Video from '../components/Video.astro';
import Navigation from '../components/Navigation.astro';
import Divider from '../components/Divider.astro';
import Illustration from '../components/Illustration.astro';
import Spacer from '../components/Spacer.astro';
---

<Layout title="Diploi – Magical Developer Experience">
  <main>
    <Navigation selected="home" />

    <article class="hero">
      <Logo />
      <h1><span class="text-gradient">Magical</span> Developer Experience</h1>
      <p>
        Single SaaS service for managing, developing and hosting your full
        application.
      </p>
      <div>
        <a class="button" href="https://console.diploi.com/">
          <HandWaving weight="fill" />
          Sign up →
        </a>
        <a class="button basic" href="#video">
          <VideoIcon weight="fill" />
          Showcase Video →
        </a>
      </div>

      <Image
        class="pipeline"
        src={pipeline}
        height={1024}
        alt="background graphic representing flow"
      />
    </article>

    <article class="quote">
      <p>
        We believe developers today are too distracted, not by choice, but due
        to the inherent complexity of today's systems. Our vision is to allow
        every developer to effortlessly get into a state of flow, completely
        free from any disruptions.
      </p>
    </article>

    <article class="features">
      <ul>
        <Card title="Consistent">
          <Target
            weight="bold"
            size={48}
            style={{ fill: 'url(#gradient-one)' }}
            slot="icon"
          />
          <li>Consistent environments from development to production</li>
          <li>Straightforward yet powerful user experience</li>
          <li>Adoptable incrementally - start easy, grow later</li>
        </Card>
        <Card title="One-Click">
          <CursorClick
            weight="bold"
            size={48}
            style={{ fill: 'url(#gradient-two)' }}
            slot="icon"
          />
          <li>Fast 30 second new developer onboarding</li>
          <li>One-click environment setup</li>
          <li>Instantly shareable testing environments</li>
        </Card>
        <Card title="Zero-Install">
          <PlugsConnected
            weight="bold"
            size={48}
            style={{ fill: 'url(#gradient-three)' }}
            slot="icon"
          />
          <li>Zero-install remote development</li>
          <li>Seamless Git integration</li>
          <li>Versatile support for any stack pre-build or custom</li>
        </Card>
      </ul>
    </article>

    <article id="video" class="flow">
      <Video />
      <!-- <Flow /> -->
    </article>

    <article class="features">
      <section>
        <h2><Gauge weight="bold" /> Full-Speed Ahead</h2>
        <p>
          Start out with a development cluster and add more developer, testing
          and production clusters as your project grows.
        </p>
      </section>

      <Spacer size="small" />

      <Illustration type="structure" />

      <Spacer size="large" />

      <section>
        <h2><DiamondsFour weight="bold" /> App Cluster</h2>
        <p>
          An app cluster hosts your full application. It can be a single web
          server or a complicated setup with databases and microservices. Use
          our premade templates to run your favourite stack.
        </p>
      </section>

      <Spacer size="small" />

      <Illustration type="cluster" />
    </article>

    <Divider />

    <article class="statistic">
      <section>
        <h2><Timer weight="bold" /> Save Time and Resources</h2>
        <p>
          These are real-life statistics from a web development company. Can you
          afford to ignore the benefits and possible savings from using Diploi?
        </p>
      </section>

      <Chart
        title="Starting a new project"
        description="in minutes avg"
        bars={[
          { title: 'Diploi', value: 7 },
          { title: 'Containerized', value: 115, comparison: 24 },
          { title: 'Traditional', value: 68, comparison: 14 },
        ]}
      />
      <Chart
        title="Onboarding a new developer"
        description="in minutes avg"
        bars={[
          { title: 'Diploi', value: 10 },
          { title: 'Containerized', value: 45, comparison: 4 },
          { title: 'Traditional', value: 92, comparison: 10 },
        ]}
      />
      <Chart
        title="Creating a new test environment"
        description="in minutes avg"
        bars={[
          { title: 'Diploi', value: 10 },
          { title: 'Containerized', value: 62, comparison: 6 },
          { title: 'Traditional', value: 115, comparison: 11 },
        ]}
      />
      <Chart
        title="Updating production"
        description="in minutes avg"
        bars={[
          { title: 'Diploi', value: 3.5 },
          { title: 'Containerized', value: 6, comparison: 2 },
          { title: 'Traditional', value: 16, comparison: 5 },
        ]}
        max={16}
      />

      <p class="saved on-scroll">
        <span>This means, that on average, Diploi saves</span>
        <strong>300 h</strong>
        <span>for every developer, every year.</span>
      </p>
    </article>

    <article class="callout">
      <section>
        <h2><AirplaneTakeoff weight="bold" /> Get Started</h2>
        <p>
          Sign up using your GitHub account and receive 50 € in free credits to
          explore and experiment with. Development is about to get
          <span class="text-gradient">magical</span>.
        </p>

        <a class="button" href="https://console.diploi.com/"
          ><HandWaving weight="fill" />Sign Up →</a
        >
      </section>
    </article>
  </main>

  <footer>
    <p>Built with ❤️ by Diploi</p>
  </footer>

  <Gradient />
</Layout>

<style>
  main {
    display: flex;
    flex-direction: column;
    align-items: center;
    color: white;
    font-size:;
  }

  article,
  section,
  footer {
    width: 100%;
    max-width: 900px;
  }

  h2 {
    display: flex;
    align-items: center;
    gap: 0.6ch;
  }

  h2 > svg {
    fill: url(#gradient-three);
  }

  .hero {
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: 90vh;
    min-height: 800px;
  }

  .hero > :global(svg) {
    flex-shrink: 0;
  }

  .hero > h1 {
    margin-bottom: 0;
    font-size: var(--font-header-size);
    font-weight: bold;
    font-family: var(--font-header);
    line-height: 1.2;
  }

  .hero > p {
    margin-bottom: 64px;
  }

  .hero > div {
    display: flex;
    gap: 16px;
  }

  @media (max-width: 900px) {
    .hero > div {
      flex-direction: column;
    }
  }

  .text-gradient {
    background-image: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
  }

  .pipeline {
    position: absolute;
    top: 0;
    right: 0;
    transform: translateX(35%) rotate(-50deg) scale(0.5);
    opacity: 0.5;
    user-select: none;
    pointer-events: none;
  }

  @media (max-width: 1000px) {
    .pipeline {
      display: none;
    }
  }

  .quote {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 48px;
  }

  .quote > p {
    font-size: 1.17em;
  }

  .quote > p::before,
  .quote > p::after {
    opacity: 0.5;
  }

  .quote > p::before {
    content: '“';
  }

  .quote > p::after {
    content: '”';
  }

  .features {
    margin-bottom: 64px;
  }

  .features,
  .flow {
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: 1200px;
  }

  .features ul {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 64px;
    margin: 0;
    margin-top: 32px;
    padding: 0;
  }

  @media (max-width: 1000px) {
    .features ul {
      grid-template-columns: 1fr;
    }
  }

  .arrow {
    margin: 64px 0;
  }

  .flow > section {
    position: relative;
    /* margin-top: -128px; */
  }

  .flow > section > * {
    position: relative;
    z-index: 1;
  }

  @media (max-width: 800px) {
    .flow > section {
      margin-top: 0;
    }
  }

  .statistic {
    display: flex;
    flex-direction: column;
    gap: 64px;
  }

  .statistic > .saved {
    display: flex;
    flex-direction: column;
    align-items: center;
    align-self: center;
    margin: 64px 0;
    text-align: center;
  }

  .statistic > .saved > strong {
    position: relative;
    font-size: 9.052rem;
    font-weight: 900;
    line-height: 1.1;
    background-image: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
  }

  .statistic > .saved > strong::after {
    content: '';
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url(/starfield.svg);
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    z-index: -1;

    transform: scale(1);
    opacity: 0;

    transition:
      transform 1s ease,
      opacity 1s ease;
  }

  .statistic > .saved.visible > strong::after {
    transform: scale(2.8);
    opacity: 0.2;
  }

  @media (max-width: 1000px) {
    .statistic > .saved > strong {
      font-size: 6.052rem;
    }

    .statistic > .saved > strong::after {
      display: none;
    }
  }

  .callout {
    position: relative;
    margin-top: 128px;
  }

  .callout h2 {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 0;
  }

  .callout h2 > svg {
    fill: url(#gradient-three);
  }

  .callout::after {
    content: '';
    display: block;
    position: absolute;
    top: -48px;
    left: -48px;
    width: calc(100% + 96px);
    height: calc(100% + 96px);
    border-radius: 24px;
    background-image: linear-gradient(
      -45deg,
      var(--color-primary),
      var(--color-secondary)
    );
    opacity: 0.1;
    z-index: -1;
  }

  .callout form {
    display: flex;
    gap: 16px;
  }

  @media (max-width: 900px) {
    .callout::after {
      display: none;
    }

    .callout form {
      flex-direction: column;
    }
  }

  footer {
    margin: auto;
    margin-top: 128px;
    font-size: 0.64rem;
    text-align: center;
    color: white;
  }

  footer p {
    max-width: 100%;
    opacity: 0.2;
  }
</style>
