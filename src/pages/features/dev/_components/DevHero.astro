---
import { features } from '../../../../content/features';
import FeaturesHero from '../../_components/FeaturesHero.astro';
import DevHeroFeatures from './DevHeroFeatures.astro';

const feature = features.find((feature) => feature.id === 'dev')!;
---

<FeaturesHero>
  <h1 slot="title">{feature.title}</h1>
  <p slot="description">Diploi is the all-in-one remote development platform — one platform from code to deployment.</p>
  <DevHeroFeatures slot="features" />
  <feature.bg weight="thin" slot="icon" />
  <feature.bg slot="mask" />
</FeaturesHero>
