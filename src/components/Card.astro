---
type Props = {
  class?: string
  isLarge?: boolean
  isCardInMobile?: boolean
}

const { class: className, isLarge, isCardInMobile = false } = Astro.props
---

<article
  class:list={[
    `card`,
    isLarge && 'large',
    isCardInMobile && 'is-card-in-mobile',
  ]}
>
  <slot />
</article>

<style lang="scss">
  @use '../styles/mixins';
  .card {
    @include mixins.card-desktop;
  }
</style>
